<header>
    <div><a href="/">SanSei</a></div>
    <nav>
        <ul>
            <% if (locals.isAuth) { %>
            <li>
                <form action="/logout" method="POST">
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                    <button>Logout</button>
                </form>
            </li>
            <% } %>
        </ul>
    </nav>
</header>